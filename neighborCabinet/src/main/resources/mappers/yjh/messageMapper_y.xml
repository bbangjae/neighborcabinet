<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neighborCabinet.project.dao.IMessageDAO_y">
	<select id="resInfo" parameterType="string" resultType="com.neighborCabinet.project.model.ReserveVO_y">
		SELECT *
		FROM reserve
		WHERE receiver=#{userId} and resState=0
	</select>
	<select id="resCheck" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM reserve
		WHERE receiver=#{userId} and reserveNo=#{reserveNo}
	</select>
	<select id="requestInfo" parameterType="hashmap" resultType="com.neighborCabinet.project.model.ReserveDetailVO_y">
		SELECT *
		FROM reserve
		WHERE reserveNo=#{reserveNo} and receiver=#{userId}
	</select>
	<update id="requestO" parameterType="int">
		UPDATE reserve
		SET resState=1
		WHERE reserveNo=#{reserveNo}
	</update>
	<insert id="requestOMessage" parameterType="hashmap">
		INSERT INTO messageAll (message, reserveNo, userId, OX)
		VALUES ("예약이 확정되었습니다.", #{reserveNo}, #{messageId}, 0)
	</insert>
	<delete id="requestX" parameterType="int">
		DELETE FROM reserve WHERE reserveNo=#{reserveNo}
	</delete>
	<insert id="requestXMessage" parameterType="hashmap">
		INSERT INTO messageAll (message, reserveNo, userId, OX)
		VALUES ("예약 신청이 거절되었습니다.", #{reserveNo}, #{messageId}, 0)
	</insert>
	<select id="messageAll" parameterType="string" resultType="com.neighborCabinet.project.model.messageAllVO_y">
		SELECT *
		FROM messageAll 
		WHERE userId=#{userId}
	</select>
</mapper >